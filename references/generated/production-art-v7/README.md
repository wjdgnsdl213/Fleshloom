# Production art v7 — quarantine 3D props

- The untouched four-cell chroma render is kept under `source/`.
- The runtime atlas contains an original response van, biomass concrete
  barrier, portable floodlight, and collapsed shelter.
- All props use the same locked 55-degree camera and screen-up-left key light
  as the directional actor sheets.
- Runtime placement is presentation-only: props have depth and occlusion but
  add no collision, capture membership, reward, or navigation rule.
